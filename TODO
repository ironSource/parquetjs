writer:
  - doc: supported encodings tables
  - ParquetWriter opts (page_size, row_group_size, compression)
  - maxbits/encoding (RLE) parameter in example
  - improved RLE encoding
  - converted type: decimal
  - converted type: time (interval)
  - nested schema
  - encodings: dictionary*, delta*
  - fixed length byte array
  - make bit_width configurable
  - checksums
  - page header v2
  - compression
  - kv metadata
  - statistics
  - index pages
  - (dictionary pages)
  - converted types: map/list, null (?)
  - split column chunk into multiple pages

reader:
  - RLE
  - remaining fromNative types
  - remaining PLAIN types
  - explicit col chunk metadata loading (if not in footer)
  - load compressed pages
  - load data pages v2
  - verify checksums
